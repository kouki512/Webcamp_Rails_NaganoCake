<h1>商品一覧</h1>


<% @items.each do |item| %>
 <div>
    <%= attachment_image_tag item, :image , :fill, 100, 100, fallback:"no_image.jpg" %>
    <%= item.name %> 
    <%= item.tax_excluded_price %>
 </div>
    
<% end %>
